<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Treasure Hunt Level 11</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  

  <style>
    body {
      background-image: url('../images/background.webp');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      min-height: 100vh;
    }
    
  </style>




</head>
<body class="bg-gray-900 flex items-center justify-center min-h-screen p-4">
  
  <% if (typeof success !== 'undefined' && success.length > 0) { %>
    <div class="z-10 absolute top-5 left-1/2 transform -translate-x-1/2 bg-green-500 text-white text-center py-2 px-4 rounded-lg">
      <%= success %>
    </div>
  <% } %>

  <% if (typeof error !== 'undefined' && error.length > 0) { %>
    <div class="z-10 absolute top-5 left-1/2 transform -translate-x-1/2 bg-red-500 text-white text-center py-2 px-4 rounded-lg">
      <%= error %>
    </div>
  <% } %>

  <!-- Main Content -->
  <div class="w-full max-w-md rounded-lg shadow-lg p-8 bg-white bg-opacity-30 backdrop-filter backdrop-blur-lg border border-gray-300">
    <h2 class="text-2xl font-semibold text-white text-center mb-6">Treasure Hunt Level 11</h2>

    <form action="/checker/level12" method="POST" class="space-y-6">
      <!-- Answer Input -->

      <div class="text-white">
        <p class="mb-2">"Only the sharpest minds will solve this. Are you one of them?"</p>
        <a class="underline" href="https://drive.google.com/drive/folders/1Wmyp5iFcwIs1-livkVbUb8x3szTDOw_n" target="_blank">Click me</a>
        <p class="mt-2">Kindly read the instructions attached.. </p>
       
      </div>

      <div>
          <label for="ans" class="block text-lm font-medium text-white">Your answer</label>
          <input id="ans" name="ans" type="text" required
          class="mt-1 block w-full px-3 py-2 bg-gray-600 text-gray-300 rounded-md 
                      focus:outline-none focus:ring-2 focus:ring-indigo-500
                      placeholder-gray-400" autocomplete="off"
               placeholder="Enter your answer">
      </div>

      <!-- Submit Button -->
      <div>
        <button type="submit" 
                class="w-full bg-indigo-600 hover:bg-indigo-700 text-white py-2 rounded-md 
                       focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
          Submit
        </button>
      </div>
    </form>
  </div>

</body>
</html>
