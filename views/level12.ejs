<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Treasure Hunt Level 12</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

  <style>
    body {
      background-image: url('../images/background.webp');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      min-height: 100vh;
    }

    .glass {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      border-radius: 10px;
      border: 1px solid rgba(255, 255, 255, 0.18);
    }
  </style>
</head>
<body class="bg-gray-900 flex items-center justify-center min-h-screen p-4">
  
  <% if (typeof success !== 'undefined' && success.length > 0) { %>
    <div class="z-10 absolute top-5 left-1/2 transform -translate-x-1/2 bg-green-500 text-white text-center py-2 px-4 rounded-lg">
      <%= success %>
    </div>
  <% } %>

  <% if (typeof error !== 'undefined' && error.length > 0) { %>
    <div class="z-10 absolute top-5 left-1/2 transform -translate-x-1/2 bg-red-500 text-white text-center py-2 px-4 rounded-lg">
      <%= error %>
    </div>
  <% } %>

  <!-- Main Content -->
  <div class="w-full max-w-md rounded-lg shadow-lg p-8 glass">
    <h2 class="text-3xl font-semibold text-white text-center mb-6">Congratulations!</h2>

    <div class="text-white">
      <p class="mb-2">"Shake hands with the treasure hunt coordinators to get the treasure..!!!"</p>
    </div>

    <!-- Optional: Button to go to the next level or main page -->
    
  </div>

</body>
</html>
