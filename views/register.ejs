<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 flex items-center justify-center h-screen">

  <% if (error && error.length > 0) { %>
    <div class="absolute top-5 left-1/2 transform -translate-x-1/2 p-4 rounded-md bg-red-500 shadow-md">
      <span class="text-white"><%= error %></span>
    </div>
  <% } %>

  <% if (success && success.length > 0) { %>
    <div class="absolute top-5 left-1/2 transform -translate-x-1/2 p-4 rounded-md bg-green-500 shadow-md">
      <span class="text-white"><%= success %></span>
    </div>
  <% } %>

  <div class="w-full max-w-md bg-gray-800 rounded-lg shadow-lg p-8 space-y-4">
    <h2 class="text-2xl font-semibold text-white text-center">Register</h2>
    <form action="/admin/register" method="post" class="space-y-6">
      <div>
        <label for="name" class="block text-sm font-medium text-gray-300">Team Name</label>
        <input id="name" name="name" type="text" required class="mt-1 block w-full px-3 py-2 bg-gray-700 text-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 transition ease-in-out duration-150">
      </div>
      <div>
        <label for="password" class="block text-sm font-medium text-gray-300">Password</label>
        <input id="password" name="password" type="password" required class="mt-1 block w-full px-3 py-2 bg-gray-700 text-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 transition ease-in-out duration-150">
      </div>
      <div>
        <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white py-2 rounded-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition ease-in-out duration-150">Register</button>
      </div>
    </form>

    <!-- Leaderboard Button -->
    <div class="mt-4">
      <a href="/admin/leaderboard" class="w-full inline-block text-center bg-gray-700 hover:bg-gray-600 text-white py-2 rounded-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition ease-in-out duration-150">
        View Leaderboard
      </a>
    </div>
  </div>

</body>
</html>
